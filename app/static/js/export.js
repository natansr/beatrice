document.querySelector('#export-form').addEventListener('submit',async event=>{event.preventDefault();const form=new FormData(event.target),status=document.querySelector('#export-status'),options={};for(const name of ['include_page_numbers','preserve_page_boundaries','include_unreviewed_pages','include_metadata','add_title_page','include_missing_page_markers'])options[name]=form.has(name);options.format=form.get('format');options.transcription=form.get('transcription');status.textContent='Gerando…';const response=await fetch(`/api/projects/${window.BEATRICE_PROJECT_ID}/export`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(options)});if(!response.ok){status.textContent=(await response.json()).detail;return}const blob=await response.blob(),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=(response.headers.get('content-disposition')?.match(/filename="?([^";]+)/)?.[1])||`beatrice.${options.format}`;a.click();URL.revokeObjectURL(url);status.textContent='Exportação concluída.'});

